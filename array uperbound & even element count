#include <stdio.h>
#include <stdlib.h>
 int u, i;
 void upperbound(void)
 { //int u;
     printf("enter an upperbound");
     scanf("%d",&u);
     if((u!=10)&&(u<10))

   {

       printf("enter an upperbound again\n");
     scanf("%d",&u);
   }

     return u;
 }
void evencount(int array[]){
int even=0;
for(i=0;i<u;i++)
{
    if((array[i]%2)==0)
    {
        even=even+1;
        printf("\n%d", i);
    }

}

printf("\n count of even numbers is : %d",even);

}


int main()
{ int size;
    upperbound();
    printf("%d\n",u);
    int array[u];

    for(i=0;i<u;i++)
    {
        printf("enter element number %d : ", i+1);
        scanf("%d",&array[i]);

    }
    for(i=0;i<u;i++)
{
    printf("%d\t", array[i]);
 }
 evencount(array);



 return 0;
}
