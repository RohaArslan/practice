#include <stdio.h>
#include <stdlib.h>

int main()
{

    int num;
    printf("enter num:");
    scanf("%d",&num);
    if(num==0)
        printf("zero");
    else if (num<0)
        printf("negative");
    else if (num>0)
        printf("positive");

    return 0;
}
